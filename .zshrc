# Export paths from elvish, which is our main shell while maintaining compatibility with applications that rely on POSIX-compliant shells to set their environment


eval "$(~/.config/elvish/export-env.elv)"
